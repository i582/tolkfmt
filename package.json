{
  "name": "tolkfmt",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -d",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "precommit": "yarn build && yarn test"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.4",
    "prettier": "^3.6.2",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "web-tree-sitter": "^0.25.6"
  }
}
